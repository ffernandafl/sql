/* Seleccionar datos */
/* EL * selecciona todos los campos */

SELECT * FROM users;

/* En vez del *, se pone el campo que quiero ver, si son más se separan por comas*/
SELECT direccion FROM users;
SELECT id, direccion FROM users;